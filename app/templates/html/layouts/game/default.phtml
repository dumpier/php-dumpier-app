@if (! presto()->http()->isAjax())
	@include('html/partials/game/header')
	@include('html/partials/game/js')

	@include('html/partials/presto.js')
	@include('html/partials/presto.css')
@endif

<div class="game-content">@content</div>

@if (! presto()->http()->isAjax())
	</body>
	</html>
@endif
