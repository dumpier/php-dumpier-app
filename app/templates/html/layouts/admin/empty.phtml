@if (! presto()->http()->isAjax())
	@include('html/partials/admin/header')
	@include('html/partials/presto.js')
	@include('html/partials/presto.css')
	<body>
@endif

@content

@if (! presto()->http()->isAjax())
	</body>
	</html>
@endif